@using MusicOrganizer.Models;

@{
  Layout = "_Layout";
}

<h1>Formats</h1>

@if (@Model.Count == 0)
{
  <h3>No formats have been added yet!</h3>
} 

@foreach (Format format in Model)
{
  <h3><a href='/formats/@format.Id'>@format.Type</a></h3>
}

<h3><a href='/'>Back Home</a></h3>